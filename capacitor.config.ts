import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kosay.constructionmanager',
  appName: 'Construction Site Manager',
  webDir: 'dist'
};

export default config;
